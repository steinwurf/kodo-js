# encoding: utf-8

jslib = bld.program(
    features='javascript cxx',
    source=bld.path.ant_glob('**/*.cpp'),
    target='kodo',
    use=['kodo_includes', 'fifi_includes'],
    export_includes=['..']
)

jslib.env.LINKFLAGS_cxxprogram += ['--bind']
